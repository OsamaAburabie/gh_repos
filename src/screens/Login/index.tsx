import {View, Text} from 'react-native';
import React from 'react';

type Props = {};

const Login = ({}: Props) => {
  return (
    <View>
      <Text>Login</Text>
    </View>
  );
};

export default Login;
